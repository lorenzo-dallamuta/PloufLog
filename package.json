{
  "name": "plouflog-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start:mobile": "npm run start --workspace=plouflog",
    "test:mobile": "npm run test --workspace=plouflog",
    "seed:wildlife": "npm run seed:wildlife --workspace=plouflog-seeding"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "overrides": {
    "pretty-format": "29.7.0"
  }
}
